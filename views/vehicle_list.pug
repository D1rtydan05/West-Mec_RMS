extends layout

block content
  h1= title

  ul
    each vehicle in vehicle_list
      li
        a(href=vehicle.url) Plate: #{vehicle.license_plate} | VIN: #{vehicle.vin}

    else
      li There are no vehicles.